export const environment = {
  production: true,
  apiURL: 'http://localhost:8080/',
  //   apiURL: 'https://markant.minibank.com/',  // Keeping this just for the sake of completetion as production url will be different based on domain name.
};
